# SPEC — الموقع الشخصي لمدرّب Front-End دولي

> **الحالة:** مسودة معتمدة بعد جلسة المقابلة (٢٠٢٦-٠٧-٣٠)
> **النطاق:** مواصفات المنتج والبنية التقنية للإصدار الأول (v1). لا يتضمن كوداً — هذا المستند هو مرجع التنفيذ.

---

## ١. نظرة عامة

### ١.١ الهدف

موقع شخصي احترافي يقدّم صاحبه كمدرّب Front-End دولي بخبرة ١٢ سنة، يعمل بنمطي التدريب **الوجاهي والأونلاين**، ويعمل كذلك **محاضراً جامعياً**. الموقع منصة تسويقية + قناة تسجيل مباشرة في الدورات، وليس منصة تعليم (LMS).

### ١.٢ أهداف قابلة للقياس

| الهدف | المؤشر |
|---|---|
| توليد تسجيلات في الدورات | عدد نماذج التسجيل المكتملة / شهر |
| بناء المصداقية | معدل الوصول لقسم الشهادات والآراء + زمن البقاء |
| الظهور في البحث بالعربية والإنجليزية | ترتيب الكلمات المفتاحية، عدد الزيارات العضوية |
| البرهنة العملية على الخبرة التقنية | Lighthouse ≥ ٩٥ في المحاور الأربعة، Core Web Vitals خضراء |

> **ملاحظة استراتيجية:** الموقع نفسه جزء من عرض القيمة. مدرّب Front-End يُحكَم على موقعه تقنياً قبل محتواه — لذلك الأداء وإمكانية الوصول ونظافة الكود متطلبات وظيفية لا تحسينات اختيارية.

### ١.٣ الجمهور المستهدف

1. **أفراد** يبحثون عن دورة Front-End (مبتدئ → متقدم)، بالعربية غالباً.
2. **شركات وجهات تدريب** تبحث عن مدرّب لبرنامج مؤسسي — تحتاج مصداقية رسمية وعرض سعر.
3. **جامعات ومراكز أكاديمية** تبحث عن محاضر/مدرّب معتمد.
4. **زملاء ومجتمع تقني** يصلون عبر المقالات — قناة SEO طويلة الأمد.

### ١.٤ خارج النطاق (v1)

- الدفع الإلكتروني وبوابات السداد.
- حسابات مستخدمين، تسجيل دخول، منطقة أعضاء.
- بث الدروس أو استضافة محتوى الدورة.
- نظام تقويم/حجز مواعيد فردية.
- لغة ثالثة.

---

## ٢. القرارات المعتمدة (خلاصة المقابلة)

| المحور | القرار |
|---|---|
| الإطار التقني | **Next.js 15 (App Router) + TypeScript + Tailwind CSS** |
| اللغة | **ثنائي اللغة: عربي RTL + إنجليزي LTR** |
| الاستضافة | **Vercel** |
| إدارة المحتوى | **Sanity (Headless CMS)** + Studio مدمج على `/studio` |
| آلية التسجيل | نموذج داخلي → إيميل + **لوحة طلبات** + **تأكيد واتساب تلقائي** |
| مزوّد الواتساب | **WhatsApp Cloud API من Meta مباشرة** |
| الاتجاه البصري | **تقني عصري داكن أولاً** مع دعم الوضع الفاتح |
| الشهادات | شبكة بطاقات بصورة + رابط تحقق رسمي |
| آراء المتدربين | **صورة (لقطة) من كلام المتدرّب** + اسم الدورة/الشعبة التي تخرّج منها |
| صفحة الدورة | المنهج + المدة + المستوى + المتطلبات + النمط + المدينة/التوقيت + الدفعات والمقاعد + السعر وعروض الشركات |
| التحليلات | Vercel Analytics + Speed Insights |
| الإضافات | SEO كامل + Schema.org + sitemap + OG images، تحميل CV، اختبارات + CI |
| المحتوى الفعلي | **غير جاهز** — محتوى تجريبي واقعي معلّم بوضوح (§١٢) |

---

## ٣. البنية التقنية

### ٣.١ حزمة التقنيات

| الطبقة | الاختيار | السبب |
|---|---|---|
| الإطار | Next.js 15 — App Router، React Server Components | SSG/ISR، SEO ممتاز، i18n عبر segment ديناميكي |
| اللغة | TypeScript (strict) | أمان الأنواع عبر طبقة البيانات والترجمات |
| التنسيق | Tailwind CSS v4 (CSS-first config) | خصائص منطقية جاهزة (`ms-`, `pe-`, `start-`) = RTL بلا جهد إضافي |
| المحتوى | Sanity v3 + GROQ + `@sanity/client` | تعريب على مستوى الحقل، Studio مدمج، ISR عبر webhook. **لا يُستورد من جذر `next-sanity`** (§10.1) |
| النماذج | React Hook Form + Zod (نفس المخطط على العميل والخادم) | تحقق موحّد، رسائل خطأ مترجمة |
| البريد | Resend + React Email | قوالب بريد بالعربية RTL والإنجليزية |
| الواتساب | WhatsApp Cloud API (Graph API v21+) | تأكيد تلقائي بقوالب معتمدة |
| الحركة | CSS + `framer-motion` عند الحاجة فقط | تقييد حجم الحزمة |
| الاختبارات | Vitest + Testing Library + Playwright + axe-core | وحدة + E2E + إمكانية وصول |
| CI/CD | GitHub Actions + Vercel Git Integration | فحص كل PR، نشر معاينة تلقائي |

### ٣.٢ هيكل المجلدات

```
.
├── src/
│   ├── app/
│   │   ├── (site)/[locale]/             # ar | en — جذر تخطيط الموقع
│   │   │   ├── layout.tsx               # يضبط lang/dir والخطوط
│   │   │   ├── page.tsx                 # الرئيسية
│   │   │   ├── about/page.tsx
│   │   │   ├── courses/
│   │   │   │   ├── page.tsx             # الكتالوج + فلاتر
│   │   │   │   └── [slug]/
│   │   │   │       ├── page.tsx         # صفحة الدورة
│   │   │   │       └── register/page.tsx# نموذج التسجيل
│   │   │   ├── certificates/page.tsx
│   │   │   ├── testimonials/page.tsx
│   │   │   ├── teaching/page.tsx        # الخبرة الأكاديمية
│   │   │   ├── blog/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [slug]/page.tsx
│   │   │   └── contact/page.tsx
│   │   ├── api/
│   │   │   ├── register/route.ts        # تسجيل في دورة
│   │   │   ├── contact/route.ts         # نموذج تواصل عام
│   │   │   └── revalidate/route.ts      # webhook من Sanity
│   │   ├── (studio)/studio/[[...tool]]/ # Sanity Studio — جذر تخطيط مستقل
│   │   ├── sitemap.ts
│   │   ├── robots.ts
│   │   └── opengraph-image.tsx          # + نسخ لكل مسار
│   ├── components/
│   │   ├── ui/                          # عناصر ذرية (Button, Card, Badge…)
│   │   ├── layout/                      # Header, Footer, LocaleSwitcher, ThemeToggle
│   │   ├── sections/                    # Hero, Stats, FeaturedCourses…
│   │   └── forms/                       # RegistrationForm, ContactForm
│   ├── lib/
│   │   ├── sanity/                      # client, queries (GROQ), image builder, types
│   │   ├── i18n/                        # config, dictionaries, getDictionary
│   │   ├── notifications/               # Notifier interface + email/whatsapp
│   │   ├── validation/                  # مخططات Zod
│   │   ├── seo/                         # metadata + JSON-LD builders
│   │   └── utils/
│   └── styles/globals.css               # رموز التصميم (design tokens)
├── sanity/
│   ├── schemas/                         # تعريف أنواع المحتوى
│   └── sanity.config.ts
├── messages/                            # ar.json | en.json (نصوص الواجهة)
├── e2e/                                 # اختبارات Playwright
├── public/                              # cv-ar.pdf, cv-en.pdf, أيقونات
└── .github/workflows/ci.yml
```

### ٣.٣ استراتيجية العرض (Rendering)

| النوع | الاستراتيجية |
|---|---|
| الصفحات الثابتة (الرئيسية، عني، الشهادات، التدريس) | SSG + ISR (`revalidate: 3600`) |
| الدورات وصفحاتها | SSG بـ `generateStaticParams` + ISR + إبطال فوري عبر webhook |
| **المقاعد المتبقية وحالة الدفعة** | جزء ديناميكي (`<Suspense>` + `dynamic = 'force-dynamic'`) أو fetch بـ `no-store` — يجب ألا تُعرض أرقام مقاعد قديمة من الكاش |
| المدونة | SSG + ISR |
| نماذج التسجيل/التواصل | Server Actions أو Route Handlers (POST فقط) |

> قرار مقصود: كل شيء ثابت افتراضياً، ويُستثنى **عدّاد المقاعد وحالة التسجيل** فقط — لأن عرض «متبقٍ ٣ مقاعد» وهي ممتلئة فعلياً خطأ منتَج لا خطأ أداء.

---

## ٤. نظام اللغتين (i18n) و RTL

### ٤.١ التوجيه

- المسارات: `/ar/...` و `/en/...`. الجذر `/` يعيد التوجيه حسب `Accept-Language` مع افتراضي **العربية**.
- `middleware.ts` يتولى: كشف اللغة، إعادة التوجيه، حفظ التفضيل في cookie (`NEXT_LOCALE`، سنة واحدة).
- مبدّل اللغة يحافظ على نفس الصفحة (`/ar/courses/react-pro` ⇄ `/en/courses/react-pro`) لا يعيد للرئيسية.

### ٤.٢ الاتجاه والخطوط

```
<html lang="ar" dir="rtl">   |   <html lang="en" dir="ltr">
```

| اللغة | الخط الأساسي | Mono |
|---|---|---|
| العربية | IBM Plex Sans Arabic (variable) | IBM Plex Mono |
| الإنجليزية | Inter (variable) | IBM Plex Mono |

- تُحمَّل عبر `next/font` (self-hosted، `display: swap`، subsetting) — لا طلبات لخوادم خطوط خارجية.
- **قاعدة إلزامية:** استخدام الخصائص المنطقية حصراً (`margin-inline-start`, `padding-inline-end`, `inset-inline-start`) وما يقابلها في Tailwind (`ms-*`, `pe-*`, `start-*`). يُمنع `left/right` إلا لعناصر لا تنعكس (شعارات المنصات، اتجاه مؤشر التمرير الأفقي في مقتطفات الكود).
- **الأرقام لاتينية في كل الواجهة العربية** (١٢٣ → 123)، لا في التواريخ
  والأسعار وحدها. صياغة هذه المواصفة الأولى قصرت القاعدة على القيم
  المنسَّقة، فظهر تعارض مرئي: عنوان الصفحة الرئيسية يقول «١٢ سنة خبرة»
  بينما شريط الإحصائيات تحته مباشرة يقول «12 سنوات خبرة» — الحقيقة نفسها
  بنظامَي ترقيم متجاورين على الشاشة. القاعدة الآن موحّدة وتشمل نصوص
  الترجمة والمحتوى، وتُنفَّذ في `Intl` بـ `latn` numbering system.
- الأيقونات الاتجاهية (الأسهم، الشيفرون) تنعكس عبر `rtl:-scale-x-100`.
- مقتطفات الكود تبقى **LTR دائماً** داخل حاوية `dir="ltr"` حتى في الصفحة العربية.

### ٤.٣ نصوص الواجهة مقابل المحتوى

| النوع | المصدر |
|---|---|
| نصوص الواجهة (أزرار، تسميات، رسائل خطأ) | `messages/ar.json` و `messages/en.json` — مفاتيح مسطّحة بأسماء وصفية |
| محتوى الموقع (الدورات، الآراء، المقالات…) | Sanity، تعريب على مستوى الحقل |

**سلوك النقص:** إذا لم تُترجم وثيقة إلى الإنجليزية، تُخفى من الكتالوج الإنجليزي ولا تُعرض بالعربية داخل صفحة إنجليزية. أما نصوص الواجهة فتسقط للعربية مع تسجيل تحذير في وضع التطوير.

---

## ٥. أقسام الموقع والصفحات

### ٥.١ الرئيسية `/[locale]`

1. **Hero** — الاسم، المسمّى («مدرّب Front-End دولي · ١٢ سنة خبرة · محاضر جامعي»)، جملة قيمة واحدة، زرّان: «استعرض الدورات» (أساسي) و«تواصل معي» (ثانوي)، وشارات صغيرة: عدد المتدربين / الدول / الجامعات.
2. **شريط الإحصائيات** — أرقام مجمّعة مع عدّاد متحرك (يُحترم `prefers-reduced-motion`).
3. **الدورات المميزة** — ٣ بطاقات مع النمط (أونلاين/وجاهي/هجين) وحالة أقرب دفعة.
4. **لماذا أنا / منهجية التدريب** — ٣–٤ نقاط قصيرة.
5. **مقتطف الشهادات** — ٤ شهادات + رابط «كل الشهادات».
6. **آراء المتدربين** — ٣ لقطات + رابط للكل.
7. **الخبرة الأكاديمية** — شعارات الجامعات + سطر واحد.
8. **أحدث المقالات** — ٣ بطاقات.
9. **CTA ختامي** — تسجيل / تواصل + زر واتساب.

### ٥.٢ عني `/[locale]/about`

سيرة مهنية سردية (Portable Text)، خط زمني للمسار المهني، منهجية التدريب، أرقام مؤثرة، صورة شخصية احترافية، **زر تحميل السيرة الذاتية PDF** (نسخة عربية ونسخة إنجليزية حسب اللغة الحالية)، وروابط التواصل الاجتماعي.

### ٥.٣ الدورات `/[locale]/courses`

- شبكة بطاقات + **فلاتر**: النمط (أونلاين/وجاهي/هجين)، المستوى (مبتدئ/متوسط/متقدم)، التقنية (React، TypeScript، Performance…)، حالة التسجيل (مفتوح فقط).
- الفلاتر تُعكس في الـ URL كـ query params (قابلة للمشاركة، صديقة للرجوع).
- كل بطاقة: صورة، العنوان، الملخص، المستوى، المدة، شارة النمط، وشارة الحالة («التسجيل مفتوح» / «مكتمل» / «قريباً»).
- حالة فارغة مصمّمة عندما لا تطابق الفلاتر شيئاً.

### ٥.٤ صفحة الدورة `/[locale]/courses/[slug]`

| الكتلة | التفاصيل |
|---|---|
| رأس الصفحة | العنوان، الملخص، شارات (المستوى، المدة بالساعات، عدد الجلسات، اللغة) |
| شريط جانبي لاصق | السعر، أقرب دفعة، المقاعد المتبقية، زر **«سجّل الآن»**، زر واتساب |
| نظرة عامة | وصف مفصّل (Portable Text) |
| مخرجات التعلّم | قائمة «ستكون قادراً على…» |
| المتطلبات المسبقة | قائمة معرفة مطلوبة |
| المنهج التفصيلي | وحدات (accordion) — كل وحدة: عنوان، مواضيع، عدد ساعات |
| النمط والمكان | أونلاين / وجاهي / هجين + المدينة والدولة + **المنطقة الزمنية** |
| الدفعات (Cohorts) | جدول: تاريخ البدء والانتهاء، الأيام والأوقات، المقاعد المتبقية، الحالة، آخر موعد للتسجيل |
| السعر | سعر الفرد + **عروض الشركات** (إما سعر أو «اطلب عرض سعر») |
| الآراء المرتبطة | لقطات آراء متدربي هذه الدورة تحديداً |
| أسئلة الدورة | أسئلة شائعة خاصة بالدورة (اختياري لكل دورة) |
| CTA سفلي | تكرار زر التسجيل للجوال |

**عرض الأوقات:** تُخزَّن أوقات الدفعة بمنطقة زمنية صريحة، وتُعرض بتوقيت الدورة **مع** توقيت الزائر المحلي: «١٩:٠٠ بتوقيت عمّان (١٨:٠٠ بتوقيتك)».

**حالات المقاعد:**

| الحالة | الشرط | السلوك |
|---|---|---|
| `open` | مقاعد > ٠ والموعد النهائي لم يمرّ | زر التسجيل فعّال |
| `few_left` | مقاعد ≤ ٣ | شارة تنبيه + الزر فعّال |
| `full` | مقاعد = ٠ | زر «انضم لقائمة الانتظار» |
| `soon` | لا تاريخ بدء محدد بعد | زر «أبلغني عند الفتح» |
| `closed` | تجاوز الموعد النهائي | معطّل مع رسالة توضيحية |

### ٥.٥ التسجيل `/[locale]/courses/[slug]/register`

راجع §٧ لتفاصيل التدفق والحقول.

### ٥.٦ الشهادات `/[locale]/certificates`

شبكة بطاقات، كل بطاقة: صورة مصغّرة للشهادة، العنوان، **الجهة المانحة**، تاريخ الإصدار (والانتهاء إن وُجد)، رقم الاعتماد، و**رابط تحقق رسمي** يفتح في تبويب جديد (`rel="noopener noreferrer"`). النقر على الصورة يفتح عارضاً (lightbox) يدعم Esc وحصر التركيز (focus trap). تجميع اختياري حسب الفئة (تقني / تدريب / أكاديمي).

### ٥.٧ آراء المتدربين `/[locale]/testimonials`

**النموذج المعتمد:** كل رأي هو **صورة/لقطة من كلام المتدرّب** (رسالة واتساب، منشور LinkedIn، تقييم…)، مرفقة بـ:

- اسم المتدرّب (أو اسم مختصر إن طلب ذلك)
- **الدورة/الشعبة التي تخرّج منها** — تُعرض كشارة وترتبط بصفحة الدورة
- تاريخ تقريبي (شهر/سنة)

**متطلبات إلزامية على هذا الشكل:**

1. **نص بديل (alt) إجباري** يحتوي فحوى الرأي — الصورة وحدها غير مقروءة لقارئ الشاشة ولا لمحركات البحث. الحقل مطلوب في مخطط Sanity ولا تُنشر الوثيقة بدونه.
2. **حقل نص مفرّغ اختياري** (`transcript`) يُعرض تحت الصورة أو في `<figcaption>` — يمنح فائدة SEO ويجعل الرأي قابلاً للبحث. موصى به بشدة لكل رأي.
3. **حقل موافقة (`consentGiven`)** إلزامي — لا تُنشر لقطة لمحادثة خاصة دون تأكيد موافقة صاحبها. هذا شرط قانوني وأخلاقي وليس حقلاً إدارياً.
4. **إخفاء البيانات الحساسة** — أرقام الهواتف وصور الملفات الشخصية في اللقطة تُطمس قبل الرفع (إرشاد للمحرر داخل الـ Studio).
5. تُعرض عبر `next/image` بـ `loading="lazy"` و`sizes` مضبوطة، وبخلفية placeholder لمنع القفز (CLS).

تخطيط masonry متجاوب + فلترة حسب الدورة. **JSON-LD:** لا تُصدَّر مراجعات Schema.org إلا للآراء التي لها `transcript` نصي فعلي — لا تُنشَأ بيانات منظمة من نص بديل وحده.

### ٥.٨ الخبرة الأكاديمية `/[locale]/teaching`

قائمة الجهات الأكاديمية: شعار الجامعة، الدور (محاضر / مدرّب معتمد)، المواد التي دُرِّست، الفترة الزمنية، المدينة والدولة، وصف قصير. قسم فرعي اختياري للمنشورات وأوراق العمل والمشاركات في المؤتمرات.

### ٥.٩ المدونة `/[locale]/blog` و `/[locale]/blog/[slug]`

- قائمة مقالات مع وسوم وزمن قراءة تقديري.
- محتوى المقال Portable Text مع كتل مخصّصة: كتلة كود (مع تمييز صياغي و**زر نسخ** واتجاه LTR قسري)، صورة بتعليق، تنبيه/ملاحظة.
- جدول محتويات لاصق للمقالات الطويلة.
- مقالات ذات صلة أسفل المقال.
- إذا لم يوجد مقابل للمقال باللغة الأخرى، يعرض مبدّل اللغة إشعاراً بدل رابط مكسور.
- **قرار:** تُخفى المدونة بالكامل من التنقل الرئيسي إذا كان عدد المقالات المنشورة صفراً — قسم مدونة فارغ يضرّ بالمصداقية أكثر مما ينفع.

### ٥.١٠ تواصل `/[locale]/contact`

نموذج تواصل عام (الاسم، الإيميل، نوع الطلب: تدريب أفراد / تدريب مؤسسي / محاضرة جامعية / أخرى، الرسالة)، بيانات مباشرة (إيميل، واتساب، LinkedIn، GitHub، YouTube)، وملاحظة عن زمن الرد المتوقع.

---

## ٦. مخططات المحتوى (Sanity)

### ٦.١ أنواع مساعدة للتعريب

```
localeString  { ar: string,  en: string }
localeText    { ar: text,    en: text }
localeBlock   { ar: block[], en: block[] }
```

قاعدة: `ar` مطلوب دائماً؛ `en` اختياري لكن غيابه يُخفي الوثيقة من الواجهة الإنجليزية.

### ٦.٢ الأنواع

**`siteSettings`** (وثيقة مفردة)
`fullName` localeString · `headline` localeString · `shortBio` localeText · `avatar` image · `cvAr` file · `cvEn` file · `email` · `whatsappNumber` (E.164) · `socials[]` {platform, url} · `stats[]` {label localeString, value number, suffix} · `defaultSeo` {title, description, ogImage}

**`course`**
`title` localeString · `slug` (مشترك للغتين) · `summary` localeText · `description` localeBlock · `level` (beginner|intermediate|advanced) · `durationHours` number · `sessionsCount` number · `language` (ar|en|both) · `prerequisites[]` localeString · `outcomes[]` localeString · `syllabus[]` {moduleTitle localeString, topics[] localeString, hours number} · `modes[]` (online|onsite|hybrid) · `cities[]` {city localeString, country} · `coverImage` image + alt · `technologies[]` string · `priceIndividual` {amount, currency} · `corporate` {available bool, showPrice bool, amount, currency} · `faqs[]` {q localeString, a localeText} · `featured` bool · `order` number · `seo` {title, description, ogImage}

**`cohort`**
`course` reference → course · `startDate` datetime · `endDate` datetime · `timezone` (IANA، مثل `Asia/Amman`) · `schedule` localeString (مثل «الأحد والثلاثاء ١٩:٠٠–٢١:٣٠») · `mode` (online|onsite|hybrid) · `city` localeString · `capacity` number · `seatsRemaining` number · `registrationDeadline` datetime · `status` (open|full|soon|closed) · `priceOverride` {amount, currency}

> `seatsRemaining` يُحدَّث يدوياً من الـ Studio في v1. عند تفعيل خصم تلقائي لاحقاً، يصبح المصدر الوحيد للحقيقة هو عدّ وثائق `registration` المؤكَّدة.

**`certificate`**
`title` localeString · `issuer` localeString · `issueDate` date · `expiryDate` date (اختياري) · `credentialId` string · `verificationUrl` url · `image` image + alt (**مطلوب**) · `category` (technical|training|academic) · `order` number

**`testimonial`**
`screenshot` image (**مطلوب**) · `altText` localeString (**مطلوب** — يصف فحوى الرأي) · `transcript` localeText (موصى به بشدة) · `traineeName` string · `traineeTitle` localeString (اختياري) · `course` reference → course · `cohortLabel` string (اسم الشعبة/الدفعة) · `date` date · `consentGiven` boolean (**مطلوب = true للنشر**) · `featured` bool

**`teachingEngagement`**
`institution` localeString · `logo` image · `role` localeString · `coursesTaught[]` localeString · `startDate` / `endDate` · `city` localeString · `country` · `description` localeText · `order`

**`post`**
`title` localeString · `slug` · `excerpt` localeText · `body` localeBlock · `coverImage` image + alt · `tags[]` · `publishedAt` datetime · `readingTime` number (يُحسب تلقائياً) · `seo`

**`registration`** (يُكتب من الـ API، مقروء من الـ Studio فقط)
`course` reference · `cohort` reference · `fullName` · `email` · `phone` (E.164) · `preferredMode` · `city` · `experienceLevel` · `notes` · `locale` · `status` (new|contacted|confirmed|cancelled) · `source` · `submittedAt` · `notificationLog` {emailSent, whatsappSent, errors[]}

**`contactMessage`** (يُكتب من الـ API)
`fullName` · `email` · `inquiryType` · `message` · `locale` · `status` · `submittedAt`

### ٦.٣ ملاحظة خصوصية

`registration` و `contactMessage` تحتويان بيانات شخصية. لا تُقرآن أبداً من الواجهة العامة — تُقرأ حصراً داخل الـ Studio بصلاحية محرر. يُمنع تضمينهما في أي استعلام يُنفَّذ بالتوكن العام.

---

## ٧. تدفق التسجيل والإشعارات

### ٧.١ تدفق المستخدم

```
صفحة الدورة → «سجّل الآن»
   → صفحة/نافذة التسجيل (الدورة والدفعة معبّأتان مسبقاً)
   → تحقق فوري على العميل (Zod)
   → POST /api/register
        ├─ تحقق على الخادم (نفس مخطط Zod)
        ├─ فحص مضاد للسبام (honeypot + الوقت + rate limit)
        ├─ التحقق من أن الدفعة ما تزال مفتوحة  ← يمنع تسجيلاً على دفعة امتلأت
        ├─ إنشاء وثيقة registration في Sanity
        ├─ إرسال إيميل للمدرّب (تنبيه) + إيميل تأكيد للمتدرّب
        └─ إرسال تأكيد واتساب عبر Cloud API
   → صفحة نجاح: ملخص التسجيل + الخطوات التالية + زر «تواصل عبر واتساب»
```

### ٧.٢ حقول النموذج

| الحقل | مطلوب | التحقق |
|---|---|---|
| الاسم الكامل | ✅ | ٢–٨٠ حرفاً |
| البريد الإلكتروني | ✅ | صيغة صحيحة، تحويل لحروف صغيرة |
| رقم الهاتف/الواتساب | ✅ | E.164 مع منتقي رمز الدولة — **إلزامي لأن التأكيد يُرسل عليه** |
| الدورة | ✅ | معبّأة مسبقاً، للقراءة |
| الدفعة | ✅ | قائمة الدفعات المفتوحة فقط |
| النمط المفضّل | ✅ | من أنماط الدورة المتاحة |
| المدينة | مشروط | مطلوب إذا كان النمط وجاهياً أو هجيناً |
| المستوى الحالي | ✅ | مبتدئ / متوسط / متقدم |
| نوع التسجيل | ✅ | فرد / نيابة عن شركة (يظهر حقل اسم الجهة) |
| ملاحظات | ❌ | ≤ ١٠٠٠ حرف |
| الموافقة على التواصل | ✅ | checkbox — موافقة صريحة على استلام تأكيد بريدي وواتساب |

### ٧.٣ طبقة الإشعارات (تجريد إلزامي)

```ts
interface Notifier {
  notifyRegistration(payload: RegistrationPayload): Promise<NotifyResult>
}
```

بتنفيذين مستقلين: `EmailNotifier` (Resend) و `WhatsAppNotifier` (Cloud API)، يُشغَّلان معاً عبر `CompositeNotifier`.

**قواعد الصلابة:**

1. **فشل الإشعار لا يُفشل التسجيل.** إذا نجحت كتابة وثيقة `registration` وفشل الواتساب، تعود الاستجابة `200` وتُسجَّل الحالة في `notificationLog`. المتدرّب سجّل فعلاً — لا يجوز إظهار خطأ له.
2. **إعادة محاولة** بتراجع أسّي (٣ محاولات: ١ث، ٤ث، ٩ث) لأخطاء 5xx والشبكة فقط، لا لأخطاء 4xx.
3. **مفتاح تكرار (idempotency key)** = هاش (البريد + معرّف الدفعة) — يمنع تسجيلين متطابقين من نقرة مزدوجة.
4. **لا تُسجَّل بيانات شخصية في اللوغات** — أرقام الهواتف والإيميلات تُقنَّع (`+9627****1234`).

### ٧.٤ WhatsApp Cloud API

**المتطلبات الإدارية (على صاحب الموقع — خارج نطاق الكود):**

1. حساب Meta Business موثّق.
2. رقم واتساب مخصّص للأعمال (لا يمكن أن يكون مستخدماً على تطبيق واتساب العادي).
3. **اعتماد قوالب الرسائل (Message Templates)** من Meta — قالبان: `registration_confirmation_ar` و `registration_confirmation_en`. الاعتماد يستغرق عادة من ساعات إلى ٢٤ ساعة وقد يُرفض ويحتاج تعديل صياغة.

**قيد جوهري:** خارج نافذة الـ ٢٤ ساعة من آخر رسالة للعميل، لا يُسمح إلا بإرسال **قوالب معتمدة** — لا نص حر. لذلك رسالة التأكيد **يجب** أن تكون قالباً بمتغيرات.

**بنية القالب المقترحة:**

```
مرحباً {{1}}، تم استلام تسجيلك في دورة {{2}}.
الدفعة تبدأ {{3}} — النمط: {{4}}.
سنتواصل معك خلال ٢٤ ساعة لتأكيد المقعد وتفاصيل الدفع.
```

**اختيار لغة القالب:** حسب `locale` الذي سجّل منه المتدرّب.

**التدهور اللطيف (Graceful degradation):** إذا لم تُضبط متغيرات البيئة الخاصة بـ Meta (بيئة تطوير، أو قبل اعتماد القوالب)، يتحوّل `WhatsAppNotifier` تلقائياً لوضع `noop` مع تسجيل تحذير، وتعرض صفحة النجاح **زر `wa.me`** يفتح واتساب برسالة معبّأة مسبقاً ليرسلها المتدرّب بنفسه. الموقع يعمل بالكامل في الحالتين ولا يتعطّل أي مسار.

### ٧.٥ مكافحة السبام

- **Honeypot** (حقل مخفي عن البشر).
- **فحص زمني** — إرسال أسرع من ٣ ثوانٍ **يُعلَّم ولا يُرفض**. تصحيح
  للخطة الأصلية: مستخدم يستعمل التعبئة التلقائية أو مدير كلمات مرور
  يرسل النموذج في ثانيتين، ورفضه يعني فقدان تسجيل حقيقي بصمت — وهو
  أسوأ من مرور رسالة سبام. الإشارة تُخزَّن في `suspectedAutomation`.
- **Rate limiting** — ٥ طلبات / ١٠ دقائق لكل IP عبر Upstash Redis (أو `@vercel/kv`).
- **Cloudflare Turnstile** — مُنفَّذ خلف علم بيئة ومعطّل افتراضياً: بلا
  مفاتيح لا يُحمَّل أي سكربت ولا يتغيّر سلوك الخادم. عند تعذّر الوصول
  لخدمة التحقق يمرّ الطلب مع تحذير بدل رفض تسجيل حقيقي بسبب عطل خارجي.

---

## ٨. نظام التصميم

### ٨.١ الاتجاه

**تقني عصري داكن أولاً.** خلفية داكنة عميقة، لون مميّز واحد بارد، تفاصيل برمجية مقصودة (خط mono للبيانات الوصفية، كتل كود في الـ Hero والمقالات، شبكة خفيفة في الخلفية)، حركة هادئة ووظيفية. الوضع الفاتح مدعوم بالكامل لا كفكرة لاحقة — كثير من الجهات الأكاديمية والشركات تفضّله عند الطباعة والعرض.

### ٨.٢ رموز التصميم (CSS Custom Properties)

```
/* الوضع الداكن — الافتراضي */
--bg:            #0B0F14
--bg-elevated:   #121821
--surface:       #161E29
--border:        #1F2A37
--text:          #E6EDF3
--text-muted:    #93A4B5
--accent:        #22D3EE   /* سماوي — أزرار أساسية وروابط */
--accent-strong: #06B6D4
--accent-soft:   rgba(34, 211, 238, 0.12)
--success:       #34D399
--warning:       #FBBF24
--danger:        #F87171

/* الوضع الفاتح */
--bg: #FFFFFF · --surface: #F6F8FA · --border: #D8DEE6
--text: #0B0F14 · --text-muted: #55606E · --accent: #0891B2
```

**تباين إلزامي:** كل ثنائي نص/خلفية ≥ **4.5:1** (نص عادي) و ≥ **3:1** (نص كبير وحدود عناصر التفاعل) في **كلا الوضعين**. `--accent` السماوي على خلفية داكنة يمرّ كنص كبير وأيقونات؛ **يُمنع استخدامه كنص صغير على الخلفية الداكنة** — تُستخدم `--text` مع تأكيد بالوزن بدلاً منه.

### ٨.٣ المقاييس

- **الطباعة:** مقياس نسبته 1.25 — 12/14/16/20/25/31/39/49px. أساس النص 16px، ارتفاع سطر 1.7 للعربية (النص العربي يحتاج تنفّساً رأسياً أكبر) و1.6 للإنجليزية.
- **المسافات:** مضاعفات 4px — 4/8/12/16/24/32/48/64/96.
- **الاستدارة:** 8px (عناصر صغيرة)، 12px (بطاقات)، 16px (كتل كبيرة)، كامل (شارات وأزرار مدوّرة).
- **الظلال:** خافتة في الوضع الداكن (الارتفاع يُنقل بالسطح والحد لا بالظل)، أوضح في الفاتح.
- **نقاط الكسر:** 480 / 768 / 1024 / 1280 / 1536. **تصميم الجوال أولاً** — الشريحة الأكبر من الجمهور العربي تتصفّح من الجوال.

### ٨.٤ الحركة

- المدد: 150ms (ريز أساسي)، 250ms (انتقالات)، 400ms (دخول الأقسام).
- `cubic-bezier(0.16, 1, 0.3, 1)` للدخول.
- **`prefers-reduced-motion: reduce` يُلغي كل الحركة والعدّادات والانزلاقات** — غير قابل للتفاوض.
- لا حركة على التمرير تسبب إزاحة تخطيط (CLS).

### ٨.٥ مكتبة المكوّنات

`Button` (primary/secondary/ghost/danger × sm/md/lg) · `Card` · `Badge` · `Tag` · `Input`/`Textarea`/`Select`/`Checkbox` (مع حالات خطأ ورسائل مرتبطة بـ `aria-describedby`) · `Accordion` (منهج الدورة والأسئلة — عنصر `<details>` الأصلي) · `Lightbox` (عنصر `<dialog>` الأصلي) · `Tabs` · `Breadcrumbs` · `Pagination` · `Skeleton` · `Toast` · `EmptyState` · `LocaleSwitcher` · `ThemeToggle` · `CodeBlock` · `StatCounter` · `TimelineItem`.

**تصحيح بعد التنفيذ:** كانت الخطة بناء العناصر التفاعلية على **Radix UI**.
عملياً غطّت عناصر المنصة الأصلية (`<details>` و `<dialog>`) كل ما احتجناه:
حصر التركيز والإغلاق بـ Esc وإعادة التركيز إلى المصدر يوفّرها المتصفح،
بكلفة صفر بايت بدل ١٥ كيلوبايت. يبقى Radix الخيار الصحيح لأي عنصر لا
يوجد له مقابل أصلي (Combobox، Tabs بسلوك لوحة مفاتيح كامل).

---

## ٩. SEO والبيانات المنظمة

### ٩.١ الميتاداتا

- `generateMetadata` لكل مسار من محتوى Sanity، مع افتراضيات من `siteSettings`.
- **`hreflang`** لكل صفحة: `ar`, `en`, و `x-default` (يشير للعربية).
- `canonical` مطلق لكل صفحة.
- Open Graph و Twitter Card كاملان.

### ٩.٢ صور المشاركة (OG)

تُولَّد ديناميكياً عبر `next/og` (ImageResponse) — 1200×630، لثلاثة مستويات:
الرئيسية، وكل صفحة دورة، وكل مقال.

**ما تبيّن عند التنفيذ** (البند كان محقّاً لكن أعمق مما وُصف):

1. **الخط**: تشكيل العربية يتطلب خطاً عربياً صريحاً، وإلا ظهرت الحروف
   منفصلة. الخط يُجلب **مرة واحدة ويُخزَّن**: البناء يولّد عشرات الصور،
   وطلب خط لكل صورة يصطدم بحدود المزوّد فتفشل بعضها — و`satori` يرمي
   خطأً حين لا يجد أي خط فيسقط البناء كله. وعند تعذّر التحميل تُرسم
   بطاقة بالهوية البصرية بلا نص بدل أن ينهار النشر.
2. **الاتجاه**: `satori` **لا ينفّذ خوارزمية Unicode ثنائية الاتجاه**.
   يشكّل حروف الكلمة العربية صحيحة، لكنه يرصّ الكلمات من اليسار إلى
   اليمين ويتجاهل `direction: rtl` تماماً، فيخرج العنوان العربي بترتيب
   كلمات مقلوب. الحل: ترتيب الكلمات يدوياً في حاوية `row-reverse`
   قابلة للالتفاف، ومحاذاة العناصر غير النصية بـ `alignSelf` صراحةً.
3. **الأجزاء (fragments)**: `satori` لا يعاملها كحاويات تخطيط فتتراكم
   العناصر فوق بعضها — كل مجموعة تحتاج حاوية `flex` صريحة.
4. **دمج الميتاداتا**: تعيين `openGraph.images` إلى `undefined` يمنع
   Next من إدراج الصورة المولّدة بالاصطلاح. يجب حذف المفتاح تماماً.
5. المسارات الديناميكية تحتاج `generateStaticParams` على ملف الصورة
   نفسه، وإلا لم تُدرج وسوم og:image في الصفحات المولّدة مسبقاً.

كل ما سبق مغطّى باختبارات E2E تتحقق من وجود الوسم وأن الصورة تُرجَع
بحجم يدل على رسم النص فعلاً.

### ٩.٣ JSON-LD (Schema.org)

| الصفحة | المخطط |
|---|---|
| كل الصفحات | `WebSite` + `Person` (المدرّب: `jobTitle`, `knowsAbout`, `alumniOf`, `sameAs`) |
| صفحة الدورة | `Course` + `CourseInstance` لكل دفعة (`courseMode`: online/onsite، `startDate`, `location`, `offers`) |
| المقال | `BlogPosting` / `TechArticle` |
| الشهادات | `EducationalOccupationalCredential` |
| الأسئلة الشائعة | `FAQPage` |
| كل الصفحات الداخلية | `BreadcrumbList` |

**قيد نزاهة:** لا يُصدَّر `AggregateRating` أو `Review` إلا من آراء لها نص مفرّغ حقيقي وموافقة مسجّلة. توليد تقييمات من نصوص بديلة أو أرقام تقديرية مخالفة لسياسات Google للبيانات المنظمة ويعرّض الموقع لعقوبة.

### ٩.٤ Sitemap و robots

`sitemap.ts` يولّد المسارات باللغتين مع `alternates.languages`، `lastModified` من `_updatedAt` في Sanity. `robots.ts` يسمح بكل شيء عدا `/studio` و `/api`.

---

## ١٠. الأداء وإمكانية الوصول

### ١٠.١ ميزانيات الأداء (تفشل الـ CI عند تجاوزها)

| المقياس | الحد |
|---|---|
| LCP | ≤ 2.0 ث (4G) |
| CLS | ≤ 0.05 |
| INP | ≤ 200 مللي ثانية |
| JS أولي (مضغوط) | ≤ 135 KB — انظر التصحيح أدناه |
| Lighthouse (الأربعة) | ≥ 95 |

> **تصحيح بعد القياس (٢٠٢٦-٠٧-٣١):** الرقم الأصلي في هذه المواصفة كان
> ١١٠ كيلوبايت، وقد وُضع قبل البناء. القياس الفعلي بيّن أن أرضية المكدّس
> وحدها ١٠٧٫٥ كيلوبايت مضغوطة (React 19 مع زمن تشغيل App Router)، أي أن
> الميزانية الأصلية تترك ٢٫٥ كيلوبايت لكل كود التطبيق — وهي غير قابلة
> للتحقيق دون تغيير الإطار. الميزانية المعتمدة الآن **١٣٥ كيلوبايت**،
> وتُفرَض آلياً بـ `npm run check:bundle` في CI على ما يحمّله كل مسار فعلاً.
>
> القياسات الحالية: الصفحات المحتوائية ١٠٧–١١٠، صفحة الشهادات ١٠٧،
> صفحتا النموذجين ١٢٩–١٣٠ (react-hook-form و zod و resolvers — كلفة
> مقصودة مقابل مخطط تحقق واحد يعمل على العميل والخادم).
>
> انحداران حقيقيان أُوقفا بهذا الفحص:
> - استيراد من جذر `next-sanity` كان يسحب مكوّنات المعاينة المباشرة إلى
>   المتصفح ويضيف **١٠٤ كيلوبايت مضغوطة إلى كل صفحة**. الحل: الاستيراد
>   من `@sanity/client` مباشرة.
> - عارض صور الشهادات كان مبنياً على Radix Dialog بكلفة ١٥ كيلوبايت.
>   الحل: عنصر `<dialog>` الأصلي، والمتصفح يوفّر حصر التركيز والإغلاق
>   بـ Esc وإعادة التركيز مجاناً.

**الوسائل:** RSC افتراضياً و`"use client"` عند الحاجة فقط · `next/image` بأبعاد صريحة و AVIF/WebP · `next/font` self-hosted · تحميل كسول لعارض الصور ومشغّلات الفيديو · تقييد `framer-motion` على المكوّنات التي تحتاجه فعلاً · تحليل الحزمة عبر `@next/bundle-analyzer` في CI.

### ١٠.٢ إمكانية الوصول — WCAG 2.1 AA

- HTML دلالي، تسلسل عناوين صحيح (`h1` واحد لكل صفحة).
- التنقل الكامل بلوحة المفاتيح مع حلقة تركيز مرئية بتباين ≥ 3:1.
- رابط «تخطَّ إلى المحتوى».
- تسميات صريحة لكل حقل + `aria-describedby` لرسائل الخطأ + `aria-live` لنتائج الإرسال.
- `alt` وصفي لكل صورة ذات معنى، و`alt=""` للزخرفية.
- **لقطات الآراء: `alt` إلزامي على مستوى المخطط** (§٥.٧) — أهم بند a11y في الموقع لأن محتوى كامل مخزّن كصور.
- احترام `prefers-reduced-motion` و `prefers-color-scheme`.
- اختبار فعلي بقارئ شاشة عربي (NVDA + Firefox) لصفحة الدورة ونموذج التسجيل.

---

## ١١. الأمان والخصوصية

- كل مدخلات المستخدم تُتحقق بـ Zod **على الخادم** — التحقق على العميل للتجربة لا للأمان.
- توكن كتابة Sanity **خادمي فقط** (`SANITY_API_WRITE_TOKEN`، بلا بادئة `NEXT_PUBLIC_`).
- webhook الإبطال محمي بتوقيع سرّي (`SANITY_REVALIDATE_SECRET`) ويُتحقق منه قبل أي عمل.
- `Content-Security-Policy` مع nonce، و`Strict-Transport-Security`، و`X-Content-Type-Options`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy`.
- Rate limiting على كل مسارات POST.
- **لا تُسجَّل بيانات شخصية** في اللوغات؛ الأرقام والإيميلات مقنّعة.
- صفحة سياسة خصوصية موجزة باللغتين تشرح: ما يُجمع، لماذا، مدة الاحتفاظ، وكيفية طلب الحذف.
- Vercel Analytics بلا كوكيز — **لا حاجة للافتة موافقة كوكيز**، وهذا سبب إضافي لاختياره.

---

## ١٢. المحتوى التجريبي (Placeholder)

المحتوى الفعلي غير جاهز. لذلك:

1. **كل قيمة تجريبية تُوسَم بالبادئة `[PLACEHOLDER]`** في حقول Sanity النصية، وتُدرج في `content/PLACEHOLDERS.md` كقائمة مرجعية للاستبدال.
2. المحتوى التجريبي **واقعي لا لوريم إيبسوم** — أسماء دورات معقولة (مثل «React الاحترافي: من المكوّنات إلى الأداء»)، منهج حقيقي البنية، شهادات بجهات مانحة حقيقية الشكل. سبب ذلك أن التخطيط يجب أن يُختبر على أطوال نص واقعية بالعربية والإنجليزية معاً — العربية أطول عادة بـ ١٥–٢٥٪ من الإنجليزية للمعنى نفسه، وهذا يكسر التخطيطات المصمَّمة على نص إنجليزي قصير.
3. الصور التجريبية بأبعاد الإنتاج نفسها لاختبار CLS فعلياً.
4. **قائمة ما يجب أن يوفّره صاحب الموقع قبل الإطلاق:**
   - الاسم الكامل، المسمّى المهني، النبذة (عربي + إنجليزي)
   - الصورة الشخصية الاحترافية
   - السيرة الذاتية PDF (نسختان)
   - الدومين المستهدف
   - بيانات الدورات الفعلية ومناهجها وأسعارها
   - صور الشهادات وروابط التحقق
   - لقطات آراء المتدربين **مع تأكيد موافقة أصحابها**
   - رقم واتساب الأعمال + حساب Meta Business
   - روابط التواصل الاجتماعي

**بوابة إطلاق:** لا يُنشر الموقع على الدومين الفعلي وفيه أي وسم `[PLACEHOLDER]` باقٍ. يُضاف فحص في CI يفشل عند وجود الوسم في بيانات بيئة الإنتاج.

---

## ١٣. الاختبارات و CI/CD

### ١٣.١ الاختبارات

| المستوى | الأداة | التغطية |
|---|---|---|
| وحدة | Vitest + Testing Library | مخططات Zod، دوال التاريخ والمنطقة الزمنية، منطق حالة المقاعد، بناة JSON-LD، `getDictionary` |
| تكامل | Vitest | مسارات `/api/register` و `/api/contact` (Sanity و Resend و Meta مموّهة) |
| E2E | Playwright | المسارات الحرجة أدناه |
| a11y | `@axe-core/playwright` | كل صفحة رئيسية × اللغتين × الوضعين — صفر مخالفات critical/serious |
| بصري | لقطات Playwright | الرئيسية وصفحة الدورة بالعربية والإنجليزية (يكشف انكسارات RTL) |

**سيناريوهات E2E الحرجة:**

1. تسجيل ناجح بالعربية (RTL) من صفحة الدورة حتى صفحة النجاح.
2. تسجيل ناجح بالإنجليزية (LTR).
3. **محاولة تسجيل على دفعة ممتلئة** ← تُرفض برسالة واضحة.
4. **فشل إشعار الواتساب** ← التسجيل ينجح ولا يرى المستخدم خطأ (§٧.٣ قاعدة ١).
5. تبديل اللغة يحافظ على الصفحة الحالية.
6. تبديل الوضع الداكن/الفاتح يثبت بعد إعادة التحميل.
7. فلاتر الدورات تنعكس في الـ URL وتعمل مع زر الرجوع.
8. تحقق النموذج: حقول ناقصة، بريد غير صالح، هاتف بصيغة خاطئة.
9. عارض صور الشهادات: يفتح، يُغلق بـ Esc، التركيز محصور ثم يعود لمصدره.
10. التنقل الكامل بلوحة المفاتيح في نموذج التسجيل.

### ١٣.٢ خط CI (GitHub Actions)

على كل PR ودفع: `lint` → `typecheck` → `test:unit` → `build` → `test:e2e` → `a11y` → فحص ميزانية الحزمة → فحص `[PLACEHOLDER]` (على فرع الإنتاج فقط). كل الفحوص إلزامية للدمج.

### ١٣.٣ النشر

- `main` → إنتاج على Vercel. كل PR → معاينة تلقائية بعنوان فريد.
- بيئات Sanity: `production` و `development` (datasets منفصلة) — لا تُجرَّب مخططات جديدة على بيانات الإنتاج.
- webhook من Sanity → `/api/revalidate` لإبطال الكاش فور نشر تعديل.
- الدومين المخصّص و SSL عبر Vercel. إعادة توجيه `www` → الجذر (أو العكس) مع 301 ثابتة.

---

## ١٤. متغيرات البيئة

| المتغير | النطاق | الوصف |
|---|---|---|
| `NEXT_PUBLIC_SITE_URL` | عام | الدومين المطلق (للـ canonical و OG) |
| `NEXT_PUBLIC_SANITY_PROJECT_ID` | عام | معرّف مشروع Sanity |
| `NEXT_PUBLIC_SANITY_DATASET` | عام | `production` \| `development` |
| `SANITY_API_READ_TOKEN` | خادم | قراءة المسودات في المعاينة |
| `SANITY_API_WRITE_TOKEN` | خادم | كتابة وثائق التسجيل والرسائل |
| `SANITY_REVALIDATE_SECRET` | خادم | توقيع webhook الإبطال |
| `RESEND_API_KEY` | خادم | إرسال البريد |
| `NOTIFICATION_EMAIL_TO` | خادم | إيميل استقبال التنبيهات |
| `WHATSAPP_PHONE_NUMBER_ID` | خادم | معرّف رقم الواتساب في Meta |
| `WHATSAPP_ACCESS_TOKEN` | خادم | توكن دائم من Meta |
| `WHATSAPP_TEMPLATE_AR` / `_EN` | خادم | أسماء القوالب المعتمدة |
| `UPSTASH_REDIS_REST_URL` / `_TOKEN` | خادم | تحديد معدّل الطلبات |
| `TURNSTILE_SITE_KEY` / `_SECRET_KEY` | مختلط | اختياري — خلف علم تفعيل |

يُوثَّق الجميع في `.env.example` بقيم وهمية. **غياب متغيرات الواتساب لا يكسر البناء** — يُفعَّل وضع `wa.me` البديل (§٧.٤).

---

## ١٥. خطة التنفيذ

| المرحلة | المخرجات |
|---|---|
| **١ — الأساس** | تهيئة Next.js + TS + Tailwind، رموز التصميم، الوضعان الداكن/الفاتح، i18n و RTL، Header/Footer/مبدّل اللغة، مكتبة UI الأساسية |
| **٢ — المحتوى** | مخططات Sanity كاملة، Studio على `/studio`، استعلامات GROQ، أنواع TypeScript مولّدة، تعبئة المحتوى التجريبي |
| **٣ — الصفحات** | الرئيسية، عني، الدورات + صفحة الدورة، الشهادات، الآراء، التدريس، المدونة، التواصل |
| **٤ — التسجيل** | نموذج التسجيل، `/api/register`، طبقة الإشعارات، قوالب البريد، تكامل الواتساب + المسار البديل، مكافحة السبام، صفحة النجاح |
| **٥ — SEO والأداء** | الميتاداتا و hreflang، JSON-LD، sitemap/robots، صور OG (**مع اختبار العربية**)، ضبط الأداء |
| **٦ — الجودة** | اختبارات الوحدة والتكامل و E2E و a11y، خط CI، فحص الميزانيات |
| **٧ — الإطلاق** | استبدال المحتوى الفعلي، ربط الدومين، اعتماد قوالب Meta، التحليلات، Search Console، مراجعة نهائية |

**المسارات الحرجة التي يجب بدؤها مبكراً لأنها تعتمد على أطراف خارجية:**

1. **اعتماد قوالب WhatsApp من Meta** — قد يستغرق أياماً ويُرفض ويحتاج إعادة صياغة. يجب تقديمه في بداية المرحلة ٤ لا نهايتها.
2. **جمع لقطات الآراء وموافقات أصحابها** — يعتمد على أشخاص آخرين، ابدأه من المرحلة ١.
3. **توثيق حساب Meta Business** — يسبق كل ما يتعلق بالواتساب.

---

## ١٦. معايير القبول (Definition of Done)

> **حالة التحقق:** البنود المؤشَّرة أدناه محققة ومغطّاة باختبارات آلية
> (٦٢ اختبار وحدة و٨٦ اختبار E2E و a11y). البنود غير المؤشَّرة تعتمد على
> محتوى فعلي أو نشر حقيقي.

- [x] الموقع يعمل كاملاً بالعربية RTL والإنجليزية LTR دون أي انكسار تخطيط في أي صفحة.
- [x] كل النصوص من ملفات الترجمة أو Sanity — لا نص مكتوب مباشرة في المكوّنات.
- [ ] Lighthouse ≥ ٩٥ في المحاور الأربعة، على الجوال، **باللغتين**.
- [x] صفر مخالفات axe من نوع critical أو serious.
- [x] تسجيل كامل ينتهي بوثيقة + إيميل للطرفين + رسالة واتساب (أو المسار البديل).
- [x] فشل الواتساب لا يمنع التسجيل ولا يُظهر خطأ للمستخدم.
- [x] لا يمكن التسجيل في دفعة ممتلئة أو مغلقة، وقائمة الانتظار تُقبل على الممتلئة وحدها.
- [x] كل لقطة رأي لها نص بديل وصفي وموافقة مسجّلة.
- [x] صور OG تعرض النص العربي بتشكيل صحيح وترتيب كلمات صحيح.
- [ ] JSON-LD يمرّ في Google Rich Results Test لصفحة الدورة والمقال.
- [x] `hreflang` و `canonical` صحيحان على كل صفحة.
- [ ] لا وسم `[PLACEHOLDER]` في بيئة الإنتاج.
- [x] كل فحوص CI خضراء محلياً (lint، types، وحدة، بناء، ميزانية، E2E، a11y).
- [x] سياسة الخصوصية منشورة باللغتين (تحتاج مراجعة قانونية قبل الإطلاق).

---

## ١٧. أسئلة مفتوحة

1. **الدومين** — لم يُحدَّد بعد؛ مطلوب قبل ضبط `NEXT_PUBLIC_SITE_URL` و canonical و OG.
2. **الأسعار والعملة** — هل يُعرض السعر علناً أم «اطلب عرض سعر» فقط؟ الحقول تدعم الحالتين، والقرار لكل دورة على حدة.
3. **حساب Meta Business** — هل هو موجود وموثّق، أم يحتاج إنشاءً من الصفر؟ يحدد جدولة المرحلة ٤.
4. **حجم المحتوى الأولي** — كم دورة وشهادة ورأياً ستتوفر عند الإطلاق؟ يؤثر على تصميم الحالات الفارغة والصفحات المقسّمة.
5. ~~**قائمة الانتظار للدفعات الممتلئة**~~ — **حُسم ونُفِّذ**: تُخزَّن
   كوثيقة `registration` بحالة `waitlist`، فتبقى البيانات جاهزة لفتح
   الدفعة التالية. الخادم يقبلها على الدفعة الممتلئة وحدها.
6. **المدونة** — من سيكتب المقالات وبأي وتيرة؟ إن لم يوجد التزام واضح، يُفضَّل تأجيل القسم للمرحلة الثانية بدل إطلاقه فارغاً.
